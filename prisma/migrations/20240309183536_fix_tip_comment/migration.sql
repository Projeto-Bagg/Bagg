BEGIN TRY

BEGIN TRAN;

-- DropIndex
ALTER TABLE [dbo].[TipComment] DROP CONSTRAINT [TipComment_tipId_key];

-- DropIndex
ALTER TABLE [dbo].[TipComment] DROP CONSTRAINT [TipComment_userId_key];

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
